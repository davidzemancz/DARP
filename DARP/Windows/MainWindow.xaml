<Window x:Class="DARP.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DARP.Windows"
        xmlns:vr="clr-namespace:DARP.Windows.ValidationRules" xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        Title="DARP" Height="900" Width="1600" Loaded="Window_Loaded">
    <Window.DataContext>
        <local:MainWindowModel/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="40*" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="40*" />
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="20*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <ToolBarTray Grid.Row="0" VerticalAlignment="Stretch" Grid.ColumnSpan="100">
            <ToolBar Band="1" Height="30">
                <Menu>
                    <MenuItem Header="_File">
                        <MenuItem Header="_Save" InputGestureText="Ctrl+S" Click="miSave_Click"/>
                        <MenuItem Header="_Open" InputGestureText="Ctrl+O" Click="miOpen_Click"/>
                    </MenuItem>
                </Menu>
            </ToolBar>
            <ToolBar Band="1" Height="30">
                <Button x:Name="btnRunSimulation" Background="CornflowerBlue" Content="Run simulation" Click="btnRunSimulation_Click"/>
            </ToolBar>
        </ToolBarTray>
        <TabControl x:Name="tbcLeft" Grid.Row="1" Grid.Column="0" SelectionChanged="tbcLeft_SelectionChanged">
            <TabItem x:Name="tbiOrders" Header="Orders" Width="100">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <ToolBarTray  Grid.Row="0" VerticalAlignment="Stretch" >
                        <ToolBar Height="30">
                            <Button Background="CornflowerBlue"  Content="Generate order" Click="newRandomOrder_Click"/>
                        </ToolBar>
                    </ToolBarTray>
                    <DataGrid Grid.Row="1" x:Name="dgOrders"/>
                </Grid>
            </TabItem>
            <TabItem x:Name="tbiVehicles" Header="Vehicles" Width="100">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <ToolBarTray  Grid.Row="0" VerticalAlignment="Stretch" >
                        <ToolBar Height="30">
                            <Button Background="CornflowerBlue"  Content="New random vehicle" Click="btwNewRandomVehicle_Click"/>
                        </ToolBar>

                    </ToolBarTray>
                    <DataGrid  Grid.Row="1"  x:Name="dgVehicles"/>
                </Grid>
            </TabItem>
            <TabItem x:Name="tbiLog" Header="Logs" Width="100">
                <Grid>
                    <TextBox x:Name="txtLog" ScrollViewer.VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" AcceptsReturn="True" Text="" IsReadOnly="True" TextChanged="txtLog_TextChanged"/>
                </Grid>
            </TabItem>
        </TabControl>
        <GridSplitter Grid.Column="1" Grid.Row="1" Width="10" HorizontalAlignment="Stretch" />
        <TabControl Grid.Row="1"  Grid.Column="2" >
            <TabItem Header="Plan" Width="100">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"/>
                        <RowDefinition/>
                        <RowDefinition Height="30"/>
                    </Grid.RowDefinitions>
                    <ToolBarTray  Grid.Row="0"  Height="30" >
                        <ToolBar Height="30">
                            <Label Content="Current time (minutes):"/>
                            <Label Content="{Binding CurrentTime}"/>
                            <Label Content="Total distance:"/>
                            <Label Content="{Binding TotalDistance}"/>
                            <Button Background="CornflowerBlue"  Content="Tick" Click="btnTick_Click"/>
                            <Button Background="CornflowerBlue"  Content="Update plan" Click="btnUpdatePlan_Click"/>
                        </ToolBar>
                    </ToolBarTray>
                    <ScrollViewer Grid.Row="1" >
                        <StackPanel x:Name="planRoutesStack"/>
                    </ScrollViewer>
                </Grid>
            </TabItem>
        </TabControl>
        <GridSplitter Grid.Column="3" Grid.Row="1" Width="10" HorizontalAlignment="Stretch" />
        <xctk:PropertyGrid  x:Name="pgSettings" Grid.Row="1" Grid.Column="4" VerticalAlignment="Stretch" SelectedObject="{Binding Params}"/>
        <ToolBar Grid.Row="2" Height="30" Grid.ColumnSpan="100">
            <!--<Label x:Name="lbSettingsStatus" Content="{Binding  Path=(Validation.Errors)[0].ErrorContent}" Width="200"/>-->
        </ToolBar>
    </Grid>

</Window>
