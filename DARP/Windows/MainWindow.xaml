<Window x:Class="DARP.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DARP"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="900" Loaded="Window_Loaded" SizeChanged="Window_SizeChanged">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ToolBarTray Height="30" Grid.ColumnSpan="3">
            <ToolBar Height="30">
                <Button Content="File"/>
            </ToolBar>
        </ToolBarTray>
        <TabControl Grid.Row="1" Grid.Column="0">
            <TabItem Header="Orders" Width="100">
                <Grid>
                    <DataGrid x:Name="dgOrders"/>
                </Grid>
            </TabItem>
            <TabItem Header="Vehicles" Width="100">
                <Grid/>
            </TabItem>
        </TabControl>
        <GridSplitter Grid.Column="1" Grid.Row="1" Width="10" HorizontalAlignment="Stretch" />
        <TabControl Grid.Row="1"  Grid.Column="2">
            <TabItem Header="Plan" Width="100">
                <Grid/>
            </TabItem>
            <TabItem Header="Logs" Width="100">
                <Grid/>
            </TabItem>
        </TabControl>
    </Grid>

</Window>
