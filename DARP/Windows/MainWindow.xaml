<Window x:Class="DARP.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DARP.Windows"
        xmlns:vr="clr-namespace:DARP.Windows.ValidationRules" xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        Title="DARP" Height="900" Width="1800" Loaded="Window_Loaded">
    <Window.DataContext>
        <local:MainWindowModel/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="45*" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="35*" />
            <ColumnDefinition Width="10"/>
            <ColumnDefinition Width="20*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <ToolBarTray Grid.Row="0" VerticalAlignment="Stretch" Grid.ColumnSpan="100">
            <ToolBar Band="0" Height="30">
                <Menu>
                    <MenuItem Header="_File">
                        <MenuItem Header="_Save" InputGestureText="Ctrl+S" Click="miSave_Click"/>
                        <MenuItem Header="_Open" InputGestureText="Ctrl+O" Click="miOpen_Click"/>
                    </MenuItem>
                </Menu>
            </ToolBar>
            <ToolBar Band="0" Height="30">
                <Button x:Name="btnRunSimulation" Background="CornflowerBlue" Content="Start simulation" Click="btnRunSimulation_Click"/>
                <Button x:Name="btnRunInsertion" Background="CornflowerBlue" Content="Run insertion heuristics" Click="btnRunInsertion_Click"/>
                <Button x:Name="btnRunMIP" Background="CornflowerBlue" Content="Run MIP solver" Click="btnRunMIP_Click"/>
                <Button x:Name="btnRunEvo" Background="CornflowerBlue" Content="Run Evolutionary solver" Click="btnRunEvo_Click"/>
                <Button x:Name="btnUpdateVehicles" Background="CornflowerBlue" Content="Update vehicles location" Click="btnUpdateVehicles_Click"/>
            </ToolBar>
            <ToolBar Band="0" Height="30">
                <Label Content="Current time (minutes):"/>
                <Label Content="{Binding CurrentTime}"/>
                <Button Background="CornflowerBlue"  Content="Tick" Click="btnTick_Click"/>
            </ToolBar>
        </ToolBarTray>
        <TabControl x:Name="tbcLeft" Grid.Row="1" Grid.Column="0" SelectionChanged="tbcLeft_SelectionChanged">
            <TabItem x:Name="tbiOrders" Header="Orders" Width="100">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <ToolBarTray  Grid.Row="0" VerticalAlignment="Stretch" >
                        <ToolBar Height="30">
                            <Button Background="CornflowerBlue"  Content="Generate order" Click="newRandomOrder_Click"/>
                        </ToolBar>
                    </ToolBarTray>
                    <DataGrid Grid.Row="1" x:Name="dgOrders"/>
                </Grid>
            </TabItem>
            <TabItem x:Name="tbiVehicles" Header="Vehicles" Width="100">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <ToolBarTray  Grid.Row="0" VerticalAlignment="Stretch" >
                        <ToolBar Height="30">
                            <Button Background="CornflowerBlue"  Content="New random vehicle" Click="btwNewRandomVehicle_Click"/>
                        </ToolBar>

                    </ToolBarTray>
                    <DataGrid  Grid.Row="1"  x:Name="dgVehicles"/>
                </Grid>
            </TabItem>
            <TabItem x:Name="tbiLog" Header="Logs" Width="100">
                <Grid>
                    <TextBox x:Name="txtLog" ScrollViewer.VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" AcceptsReturn="True" Text="" IsReadOnly="True" TextChanged="txtLog_TextChanged"/>
                </Grid>
            </TabItem>
        </TabControl>
        <GridSplitter Grid.Column="1" Grid.Row="1" Width="5" HorizontalAlignment="Stretch" />
        <TabControl Grid.Row="1"  Grid.Column="2" >
            <TabItem Header="Routes" Width="100">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <ScrollViewer Grid.Row="0" >
                        <StackPanel x:Name="planRoutesStack"/>
                    </ScrollViewer>
                </Grid>
            </TabItem>
            <TabItem Header="Statistics"  Width="100">
                <StackPanel>
                    <Label Content="{Binding Stats.TotalOrdersStr}"/>
                    <Label Content="{Binding Stats.HandledOrdersStr}"/>
                    <Label Content="{Binding Stats.AcceptedOrdersStr}"/>
                    <Label Content="{Binding Stats.RejectedOrdersStr}"/>
                    <Separator/>
                    <Label Content="{Binding Stats.TotalTimeTraveledStr}"/>
                    <Label Content="{Binding Stats.TotalProfitStr}"/>
                </StackPanel>
            </TabItem>
            <TabItem Header="Map"  Width="100">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="100*"/>
                        <RowDefinition Height="60"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5" Height="20">
                        <Button x:Name="btnRefreshMap" Content="Refresh" Click="btnRefreshMap_Click" Padding="5, 0"  VerticalAlignment="Center"/>
                        <CheckBox x:Name="chbDrawOrders" Content="Orders" Margin="10,0" IsChecked="True"  VerticalAlignment="Center"/>
                        <CheckBox x:Name="chbDrawRoutes" Content="Routes" Margin="10,0" IsChecked="True"  VerticalAlignment="Center"/>
                    </StackPanel>
                    <Canvas Grid.Row="1" x:Name="cMap" Margin="20"/>
                    <GroupBox Grid.Row="2" Header="Legend">
                        <Canvas x:Name="cLegend"/>
                    </GroupBox>
                </Grid>
            </TabItem>
        </TabControl>
        <GridSplitter Grid.Column="3" Grid.Row="1" Width="5" HorizontalAlignment="Stretch" />
        <TabControl  Grid.Row="1" Grid.Column="4">
            <TabItem Header="Properties" Width="100">
                <xctk:PropertyGrid  x:Name="pgSettings" VerticalAlignment="Stretch" SelectedObject="{Binding Params}"/>
            </TabItem>
            <TabItem Header="Tasks" Width="100">
                <ListBox x:Name="lbTasks" ItemsSource="{Binding Tasks}"/>
            </TabItem>
        </TabControl>
        <ToolBar Grid.Row="2" Height="30" Grid.ColumnSpan="100">
            
        </ToolBar>
    </Grid>

</Window>
