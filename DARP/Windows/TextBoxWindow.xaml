<Window x:Class="DARP.Windows.TextBoxWindow" x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DARP.Windows"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Loaded="Window_Loaded"
        KeyDown="Window_KeyDown"
        Title="TextBoxWindow" Height="146" Width="432">
    <Grid DockPanel.Dock="Top">
        <Grid.RowDefinitions>
            <RowDefinition Height="100*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100*"/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="txtVal" Margin="10,35,10,0" TextWrapping="Wrap" Text="" VerticalAlignment="Top"/>
        <Label x:Name="lbDesc" Content="Value" HorizontalAlignment="Left" Margin="10,10,0,0" Padding="2" Height="20" VerticalAlignment="Top" />
        <Button x:Name="btnOk" Content="OK" HorizontalAlignment="Right" Margin="0,0,10,10" Padding="5" VerticalAlignment="Bottom" Width="74" Click="btnOk_Click" RenderTransformOrigin="0.032,0.562"/>
        <Button x:Name="btnCancel" Content="Cancel" HorizontalAlignment="Right" Margin="0,0,88,10" Padding="5"  VerticalAlignment="Bottom" Width="72" Click="btnCancel_Click" />
    </Grid>
</Window>
